
@{
    Layout=null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Home</title>
    <link href="~/Contents/bootstrap-4.5.3-dist/css/bootstrap.css" rel="stylesheet" />
</head>
<body>
    
    <div>

        
        <nav>
            @Html.ActionLink("booking", "booking", new { @class = "btn btn-primary" })
            @Html.ActionLink("profile", "profile")
        </nav>

        @if (Session["user"] == null)
        {
            @Html.ActionLink("Login", "Login")
        }
        else
        {
            @Html.ActionLink("Logout", "Logout")
        }
    </div>
</body>
</html>
