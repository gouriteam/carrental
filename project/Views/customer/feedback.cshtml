@model BusinessEntity.feedbackBE
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>

    <meta name="viewport" content="width=device-width" />
    <title>feedback</title>
</head>
<body>
    <form method="post">
        <table>
            @*<tr>
                    <th> id </th>

                    <th> bookingid </th>

                    <th> comments </th>

                    <th> ratings </th>
                    </tr>

                <tr>*@
            <td> Enter id </td>
            <td>@Html.TextBoxFor(a => a.id)</td>
            </tr>
            <tr>
                <td> Enter bookingid </td>
                <td>@Html.TextBoxFor(a => a.bookingid)</td>
            </tr>
            <tr>
                <td> comments </td>
                <td>@Html.TextBoxFor(a => a.comments)</td>
            </tr>
            <tr>
                <td> Give Ratings Here </td>
                <td>@Html.TextBoxFor(a => a.rating)</td>
            </tr>
            <tr>
                <td></td>
                <td><input type="submit" value="Submit" /></td>
            </tr>
            @*<p>@{ var star = Convert.ToInt32(item.Value); }</p>
                @for (int i = 0; i < star; i++)
                {
                    <select id="Star" name="Star" class="jqStar">
                        <option value=""></option>
                        <option value="@i">1</option>
                        <option value="@i">2</option>
                        <option value="@i">3</option>
                        <option value="@i">4</option>
                        <option value="@i">5</option>
                    </select>
                }*@
        </table>
    </form>
    @ViewData["a"]
</body>
</html>
